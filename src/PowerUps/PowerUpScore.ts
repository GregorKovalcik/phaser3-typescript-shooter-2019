    import { PowerUp } from "./PowerUp";
    
    export class PowerUpScore extends PowerUp {
  
        constructor( scene: Phaser.Scene ) {
            super(scene, "powerupScore");
        }
        
    }
