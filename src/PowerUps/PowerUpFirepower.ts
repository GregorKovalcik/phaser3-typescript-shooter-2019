    import { PowerUp } from "./PowerUp";
    
    export class PowerUpFirepower extends PowerUp {
  
        constructor( scene: Phaser.Scene ) {
            super(scene, "powerupFirepower");
        }
        
    }
